import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bookplacer.bookmarket',
  appName: 'ionicbookplacer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
